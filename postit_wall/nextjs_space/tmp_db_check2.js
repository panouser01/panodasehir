const { PrismaClient } = require(@prisma/client); const prisma = new PrismaClient(); async function main() { await prisma.siteSettings.upsert({ where: { id: global }, update: { postitAppearance: { shapeType: circle } }, create: { id: global, postitAppearance: { shapeType: circle } } }); const res = await prisma.siteSettings.findUnique({ where: { id: global } }); console.log(res.postitAppearance); } main().finally(async () => { await prisma.(); });
